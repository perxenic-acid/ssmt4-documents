所有权: 确保安全机制

安全: 无未定义

```Rust
fn read(y: bool) {
    if y {
        println!("是真的!");
    }
}
```
```rust
fn main() {
    let x = true;
    read(x);
}
```
```rust
fn main() {
    read(x);
    let x = true;
}
```
其他语言: 运行时;
rust: 编译时
程序崩溃? 数据危险!

基础目标: 程序永远无未定义行为
次要目标: 尽可能在编译时而非运行时阻止未定义行为.

栈帧